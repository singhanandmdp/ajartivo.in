# Admin Firebase Backup

This folder stores the old Firebase-based admin code that was removed from the active `admin` folder after the Supabase migration.

Files kept here:

- `firebase-config.js`
- `script.js`
